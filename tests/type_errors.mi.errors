# Expected errors (one per line, matched as substring)
# Lines starting with # are comments

# Wrong arg to annotated function (reducer + type checker merged)
cannot multiply string; bad1: expected Int, got Str

# Wrong arg to ADT-typed function (reducer + type checker merged)
no pattern matches this value; bad2: expected Shape

# Prelude type violations
bad3: expected a : b : a, got Int
bad4: expected a : b, got Str
bad5: expected List, got Int
bad6: expected List, got Str

# Operand type errors (reducer + type checker merged)
cannot multiply string; bad7: operator *
cannot subtract from string; bad8: operator -

# Record field type mismatch
p: expected Int, got Str
