# Expected trait errors (substring matches)

# Fix 1: World aliasing detected
should_fail: effect violation

# Fix 7: Rebound println keeps effect tracking
rebind_test: effect violation

# Trait inference: unannotated functions are NOT checked, but their effects
# propagate to annotated callers
bad_caller: effect violation
