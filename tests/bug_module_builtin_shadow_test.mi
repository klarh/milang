m = import "lib/bug_shadow_module.mi"
main world =
  -- Test: module's trim correctly wraps the builtin via import "prelude"
  world.io.println (m.build (m.trim (m.from "  hello  ")))
  -- Expected: "hello"
