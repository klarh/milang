-- Test: user shadow of fold doesn't affect imported module.
-- Module has its own prelude via import "prelude", so user's fold is irrelevant.
fold x = x

m = import "lib/double_sum.mi"

main world =
  world.process.exit (m.double_sum [1, 2, 3] - 12)
